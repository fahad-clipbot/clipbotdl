# 🚀 تشغيل البوت
# Start the Bot

دليل تشغيل بوت ClipBotDL مع نظام الاشتراكات PayPal.

---

## ✅ التحقق من الإعدادات

### 1. تحقق من ملف .env

```bash
cat .env
```

يجب أن تجد:
```
TELEGRAM_BOT_TOKEN=8509142185:AAH3UX6uH_3q-Tb6KYbjqAqjrpga41m7hqE
PAYPAL_CLIENT_ID=Afc7nu7o97GforFoMfGYiV2dvaIppnSdryPKi4C111Zn2-2CYgo4Hqv8l9KkPpIAgHPY9Yxkp_lq-DBB
PAYPAL_CLIENT_SECRET=EDEf_8Jc7he9dWs-iutUGeiqXyrnYosmW8pbKiOjQXWYgNCo-I3_AswrRnW3e3GQBVYv1Yx97jgekqrk
PAYPAL_MODE=sandbox
```

### 2. تحقق من المكتبات

```bash
pip install -r requirements.txt
```

---

## 🚀 تشغيل البوت

### الطريقة 1: تشغيل مباشر (Linux/Mac)

```bash
python3 bot_with_paypal.py
```

### الطريقة 2: استخدام ملف التشغيل (Linux/Mac)

```bash
chmod +x run.sh
./run.sh
```

### الطريقة 3: تشغيل على Windows

```bash
python bot_with_paypal.py
```

أو استخدم:
```bash
run.bat
```

---

## 📱 اختبر البوت

### 1. افتح تليجرام

```
ابحث عن: @ClipBotDLBot
```

### 2. أرسل /start

```
/start
```

### 3. يجب أن تظهر الرسالة:

```
👋 أهلاً وسهلاً في ClipBotDL!

🎬 هذا البوت يساعدك على تنزيل الفيديوهات من:
   • YouTube
   • TikTok
   • Instagram

📦 الخطط المتاحة:
   • مجاني: 5 تنزيلات/يوم
   • أساسي: $2.99/شهر
   • احترافي: $4.99/شهر
   • متقدم: $9.99/شهر

اختر من الخيارات أدناه:
```

### 4. الأوامر المتاحة:

```
/start       - ابدأ البوت
/subscribe   - عرض الخطط
/status      - حالة الاشتراك
/help        - المساعدة
/stats       - الإحصائيات
```

---

## 🧪 اختبر الاشتراك

### 1. اختر خطة

```
انقر على: "📦 عرض الخطط"
```

### 2. اختر "أساسي" ($2.99)

```
انقر على: "اشترك في الأساسي"
```

### 3. سيفتح رابط PayPal

```
سيظهر صفحة PayPal للدفع
```

### 4. استخدم بيانات الاختبار

في Sandbox، استخدم:
```
البريد: sb-xxxxx@personal.example.com
كلمة المرور: 123456
```

أو استخدم بطاقة اختبار:
```
الرقم: 4111111111111111
الشهر: 12
السنة: 2025
CVV: 123
```

### 5. أكمل الدفع

```
انقر على: "Complete Purchase"
```

### 6. تحقق من الاشتراك

```
أرسل: /status
يجب أن يظهر: "✅ مشترك في الخطة: أساسي"
```

---

## 📊 الأوامر الإدارية

### عرض الإحصائيات

```bash
python3 << 'EOF'
from database_models import Database

db = Database()
stats = db.get_statistics()

print(f"إجمالي المستخدمين: {stats['total_users']}")
print(f"الاشتراكات النشطة: {stats['active_subscriptions']}")
print(f"إجمالي الإيرادات: ${stats['total_revenue']:.2f}")
EOF
```

### عرض المستخدمين

```bash
python3 << 'EOF'
from database_models import Database

db = Database()
users = db.get_all_users()

for user in users:
    print(f"المستخدم: {user['first_name']} {user['last_name']}")
    print(f"معرف: {user['telegram_id']}")
    print(f"اسم المستخدم: {user['username']}")
    print("---")
EOF
```

---

## ⚠️ حل المشاكل الشائعة

### ❌ "Token not found"

```
✅ الحل:
1. تحقق من ملف .env
2. تأكد من وجود TELEGRAM_BOT_TOKEN
3. تأكد من أن الرمز صحيح
```

### ❌ "PayPal Connection Error"

```
✅ الحل:
1. تحقق من الاتصال بالإنترنت
2. تحقق من Client ID و Secret
3. شغّل: python3 test_paypal_connection.py
```

### ❌ "Bot not responding"

```
✅ الحل:
1. تحقق من أن البوت يعمل
2. تحقق من الرسائل في Terminal
3. أعد تشغيل البوت
```

### ❌ "Database locked"

```
✅ الحل:
1. احذف ملف subscriptions.db
2. أعد تشغيل البوت
3. سيتم إنشاء قاعدة بيانات جديدة
```

---

## 🔄 إيقاف البوت

### على Linux/Mac

```bash
Ctrl + C
```

### على Windows

```
Ctrl + C
```

---

## 📝 ملاحظات مهمة

### ✅ افعل:
- ✅ احفظ .env في مكان آمن
- ✅ استخدم Sandbox أولاً
- ✅ اختبر كل شيء قبل الإنتاج
- ✅ راقب البوت أثناء التشغيل

### ❌ لا تفعل:
- ❌ لا تشارك .env مع أحد
- ❌ لا تستخدم Live مباشرة
- ❌ لا تحذف subscriptions.db
- ❌ لا تغير الكود أثناء التشغيل

---

## 🎉 تم!

البوت الآن يعمل! 🚀

**الخطوة التالية:**
1. اختبر البوت محلياً
2. تأكد من عمل الاشتراكات
3. نشّر البوت على الإنترنت

---

**هل تحتاج إلى مساعدة؟**

اقرأ:
- PAYPAL_QUICK_START.md
- PAYPAL_SETUP.md
- README.md

أو شغّل:
- test_paypal_connection.py
- test_bot.py
