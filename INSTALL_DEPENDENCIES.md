# 📦 دليل تثبيت المكتبات المطلوبة

## المكتبات الأساسية

```bash
pip install python-telegram-bot requests python-dotenv
```

## مكتبات تنزيل الوسائط

### الخيار 1: استخدام yt-dlp (الموصى به للبدء)

```bash
pip install yt-dlp
```

**المميزات:**
- ✅ يدعم يوتيوب بشكل ممتاز
- ✅ يدعم تيك توك وانستقرام (مع تحديثات)
- ✅ سهل التثبيت

**العيوب:**
- ❌ قد يحتاج تحديثات متكررة
- ❌ قد يفشل مع الروابط المحظورة

---

### الخيار 2: استخدام instagrapi (للانستقرام)

```bash
pip install instagrapi
```

**المميزات:**
- ✅ متخصص في انستقرام
- ✅ يدعم جميع أنواع الوسائط
- ✅ موثوق جداً

**المتطلبات:**
- حساب انستقرام (اختياري للمحتوى العام)

---

### الخيار 3: استخدام TikTok API

```bash
pip install TikTokApi
# أو
pip install pytiktok
```

**المميزات:**
- ✅ متخصص في تيك توك
- ✅ دعم كامل للفيديوهات والصور

---

### الخيار 4: استخدام instagram-scraper

```bash
pip install instagram-scraper
```

**المميزات:**
- ✅ بديل موثوق لـ instagrapi
- ✅ لا يحتاج حساب

---

## المكتبات الإضافية

### FFmpeg (مطلوب لمعالجة الفيديو والصوت)

#### على Windows:
```bash
# باستخدام Chocolatey
choco install ffmpeg

# أو تحميل يدوي من:
# https://ffmpeg.org/download.html
```

#### على Linux (Ubuntu/Debian):
```bash
sudo apt-get install ffmpeg
```

#### على macOS:
```bash
brew install ffmpeg
```

---

## التثبيت الكامل (الموصى به)

```bash
# المكتبات الأساسية
pip install python-telegram-bot requests python-dotenv

# مكتبات تنزيل الوسائط
pip install yt-dlp instagrapi

# مكتبات إضافية
pip install paypalrestsdk  # للدفع
pip install SQLAlchemy     # لقاعدة البيانات
```

---

## التحقق من التثبيت

```bash
# التحقق من yt-dlp
python3 -c "import yt_dlp; print('yt-dlp OK')"

# التحقق من instagrapi
python3 -c "import instagrapi; print('instagrapi OK')"

# التحقق من FFmpeg
ffmpeg -version
```

---

## استكشاف الأخطاء

### خطأ: "No module named 'yt_dlp'"

```bash
pip install --upgrade yt-dlp
```

### خطأ: "FFmpeg not found"

تأكد من تثبيت FFmpeg وإضافته إلى PATH

### خطأ: "Permission denied"

```bash
pip install --user yt-dlp instagrapi
```

---

## الإصدارات الموصى بها

| المكتبة | الإصدار | الحالة |
|--------|---------|--------|
| python-telegram-bot | 20.x+ | ✅ |
| yt-dlp | 2023.x+ | ✅ |
| instagrapi | 2.0.x+ | ✅ |
| requests | 2.28.x+ | ✅ |
| FFmpeg | 5.x+ | ✅ |

---

## نصائح للأداء الأفضل

1. **حدث المكتبات بانتظام:**
   ```bash
   pip install --upgrade yt-dlp
   ```

2. **استخدم بيئة افتراضية:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # على Linux/macOS
   venv\Scripts\activate     # على Windows
   ```

3. **تثبيت المتطلبات من ملف:**
   ```bash
   pip install -r requirements.txt
   ```

---

## الدعم والمساعدة

إذا واجهت مشاكل:

1. تحقق من إصدار Python (3.8+)
2. حدث جميع المكتبات
3. تحقق من اتصال الإنترنت
4. جرب في بيئة افتراضية نظيفة

---

**آخر تحديث**: نوفمبر 2025
