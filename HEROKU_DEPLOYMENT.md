# 🚀 نشر البوت على Heroku

## الخطوة 1: إنشاء حساب Heroku

1. اذهب إلى: https://www.heroku.com
2. اضغط على "Sign Up"
3. ملأ البيانات:
   - البريد الإلكتروني
   - كلمة المرور
   - الاسم الأول
4. اختر: **Developer** كنوع الحساب
5. تحقق من بريدك الإلكتروني

---

## الخطوة 2: تثبيت Heroku CLI

### على Windows:
```bash
# تحميل المثبت من:
https://devcenter.heroku.com/articles/heroku-cli

# أو استخدم Chocolatey:
choco install heroku-cli
```

### على Mac:
```bash
brew tap heroku/brew && brew install heroku
```

### على Linux:
```bash
curl https://cli-assets.heroku.com/install.sh | sh
```

---

## الخطوة 3: تسجيل الدخول إلى Heroku

```bash
heroku login
```

سيفتح متصفح لتسجيل الدخول. اضغط على "Log In"

---

## الخطوة 4: إنشاء تطبيق Heroku

```bash
# انتقل إلى مجلد المشروع
cd telegram_bot_project

# إنشاء تطبيق جديد
heroku create clipbotdl

# أو اترك Heroku تختار اسماً عشوائياً
heroku create
```

---

## الخطوة 5: إضافة متغيرات البيئة

```bash
# أضف متغيرات البيئة
heroku config:set TELEGRAM_BOT_TOKEN="8509142185:AAH3UX6uH_3q-Tb6KYbjqAqjrpga41m7hqE"
heroku config:set PAYPAL_CLIENT_ID="Afc7nu7o97GforFoMfGYiV2dvaIppnSdryPKi4C111Zn2-2CYgo4Hqv8l9KkPpIAgHPY9Yxkp_lq-DBB"
heroku config:set PAYPAL_CLIENT_SECRET="EDEf_8Jc7he9dWs-iutUGeiqXyrnYosmW8pbKiOjQXWYgNCo-I3_AswrRnW3e3GQBVYv1Yx97jgekqrk"
heroku config:set PAYPAL_MODE="sandbox"
```

---

## الخطوة 6: نشر البوت

```bash
# إضافة الملفات
git add .
git commit -m "Deploy bot to Heroku"

# نشر البوت
git push heroku main
```

أو إذا كنت تستخدم `master`:
```bash
git push heroku master
```

---

## الخطوة 7: تشغيل البوت

```bash
# تشغيل البوت
heroku ps:scale worker=1

# التحقق من الحالة
heroku ps

# عرض السجلات
heroku logs --tail
```

---

## الخطوة 8: التحقق من البوت

1. افتح تليجرام
2. ابحث عن: **@ClipBotDLBot**
3. أرسل: **/start**
4. يجب أن ترى الرسالة الترحيبية

---

## الأوامر المفيدة

```bash
# عرض السجلات
heroku logs --tail

# إعادة تشغيل البوت
heroku ps:restart

# إيقاف البوت
heroku ps:scale worker=0

# تشغيل البوت مرة أخرى
heroku ps:scale worker=1

# حذف التطبيق
heroku apps:destroy --app clipbotdl
```

---

## استكشاف الأخطاء

### المشكلة: البوت لا يستجيب

**الحل:**
```bash
# تحقق من السجلات
heroku logs --tail

# أعد تشغيل البوت
heroku ps:restart
```

### المشكلة: خطأ في المتغيرات

**الحل:**
```bash
# تحقق من المتغيرات
heroku config

# أضف متغير جديد
heroku config:set KEY=VALUE
```

### المشكلة: البوت يتوقف بعد دقائق

**الحل:**
```bash
# تأكد من أن الملفات صحيحة
# تحقق من Procfile و requirements.txt

# أعد النشر
git push heroku main
```

---

## ملاحظات مهمة

✅ **Heroku توفر:**
- 550 ساعة شهرية مجانية
- كافية لتشغيل البوت 24/7
- بدون بطاقة ائتمان في البداية

✅ **الملفات المطلوبة:**
- `Procfile` - يحدد كيفية تشغيل البوت
- `requirements.txt` - المكتبات المطلوبة
- `runtime.txt` - إصدار Python

✅ **متغيرات البيئة:**
- لا تضع المفاتيح في الكود
- استخدم `heroku config:set` لإضافتها

---

## الدعم

إذا واجهت مشكلة:
1. تحقق من السجلات: `heroku logs --tail`
2. اقرأ رسالة الخطأ بعناية
3. تأكد من أن جميع الملفات موجودة

---

**تم! البوت الآن على Heroku ويعمل 24/7!** 🚀
