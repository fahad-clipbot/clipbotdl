# 📁 هيكل المشروع
# Project Structure

شرح تفصيلي لهيكل ملفات المشروع وكيفية عمل كل ملف.

## 🗂️ الهيكل الكامل

```
telegram_bot_project/
├── bot.py                      # البوت الأساسي (نسخة بسيطة)
├── bot_improved.py             # البوت المحسّن (الموصى به)
├── config.py                   # ملف الإعدادات المركزي
├── downloader.py               # وحدة تنزيل الفيديوهات
├── requirements.txt            # المكتبات المطلوبة
├── .env                        # متغيرات البيئة (سري)
├── .gitignore                  # ملفات Git المتجاهلة
├── run.sh                      # ملف التشغيل على Linux/Mac
├── run.bat                     # ملف التشغيل على Windows
├── README.md                   # معلومات عامة عن المشروع
├── QUICK_START.md              # دليل البدء السريع
├── INSTALLATION.md             # دليل التثبيت التفصيلي
├── USAGE.md                    # دليل الاستخدام الشامل
├── STRUCTURE.md                # هذا الملف
├── downloads/                  # مجلد الفيديوهات المنزلة
└── venv/                       # البيئة الافتراضية (لا تشارك)
```

---

## 📄 شرح الملفات الرئيسية

### 1. `bot.py` - البوت الأساسي

**الغرض:** نسخة بسيطة وسهلة الفهم من البوت

**المحتوى:**
- فئة `VideoDownloader` لتنزيل الفيديوهات
- معالجات الأوامر (`/start`, `/help`, `/stats`)
- معالج الروابط المرسلة من المستخدمين
- معالج الأخطاء

**الاستخدام:**
```bash
python3 bot.py
```

**الميزات:**
- سهل الفهم للمبتدئين
- يحتوي على جميع الميزات الأساسية
- مناسب للتعديل والتطوير

---

### 2. `bot_improved.py` - البوت المحسّن

**الغرض:** نسخة محسّنة وأكثر تنظيماً من البوت

**المحتوى:**
- فئة `TelegramVideoBot` لإدارة البوت
- معالجات محسّنة للأوامر
- نظام إحصائيات المستخدمين
- معالجة أخطاء متقدمة
- تسجيل شامل للعمليات (Logging)

**الاستخدام:**
```bash
python3 bot_improved.py
```

**الميزات:**
- كود منظم وقابل للصيانة
- نظام إحصائيات متقدم
- معالجة أخطاء شاملة
- تسجيل تفصيلي للعمليات

**الفرق عن `bot.py`:**
- استخدام فئة (Class) لتنظيم الكود
- نظام إحصائيات لكل مستخدم
- معالجة أخطاء أفضل
- تسجيل تفصيلي (Logging)

---

### 3. `config.py` - ملف الإعدادات

**الغرض:** إدارة جميع إعدادات البوت في مكان واحد

**المحتوى:**
- متغيرات البيئة (`BOT_TOKEN`, `DOWNLOAD_FOLDER`)
- حدود الملفات (`MAX_FILE_SIZE`, `MIN_FILE_SIZE`)
- إعدادات التنزيل (`DOWNLOAD_TIMEOUT`, `SOCKET_TIMEOUT`)
- إعدادات السجلات (`LOG_LEVEL`, `LOG_FORMAT`)
- معلومات المنصات المدعومة
- الرسائل المستخدمة في البوت
- دالة التحقق من الإعدادات (`validate_config`)

**الاستخدام:**
```python
from config import BOT_TOKEN, DOWNLOAD_FOLDER, MAX_FILE_SIZE
```

**الفائدة:**
- تغيير الإعدادات بسهولة دون تعديل الكود الرئيسي
- إدارة مركزية للثوابت
- سهولة الصيانة والتطوير

---

### 4. `downloader.py` - وحدة التنزيل

**الغرض:** فصل منطق التنزيل عن البوت الرئيسي

**المحتوى:**
- فئة `VideoDownloader` مع الوظائف التالية:
  - `is_youtube_url()` - التحقق من روابط يوتيوب
  - `is_tiktok_url()` - التحقق من روابط تيك توك
  - `is_instagram_url()` - التحقق من روابط انستقرام
  - `download_youtube()` - تنزيل من يوتيوب
  - `download_tiktok()` - تنزيل من تيك توك
  - `download_instagram()` - تنزيل من انستقرام
  - `download_video()` - تنزيل من أي منصة
  - `get_file_size_mb()` - الحصول على حجم الملف
  - `cleanup_file()` - حذف الملف

**الاستخدام:**
```python
from downloader import VideoDownloader

# التحقق من الرابط
if VideoDownloader.is_youtube_url(url):
    filename, platform = VideoDownloader.download_video(url)
```

**الفائدة:**
- فصل المسؤوليات (Separation of Concerns)
- إعادة استخدام الكود
- سهولة الاختبار والصيانة

---

### 5. `requirements.txt` - المكتبات المطلوبة

**الغرض:** تحديد جميع المكتبات المطلوبة للمشروع

**المحتوى:**
```
python-telegram-bot==22.5
yt-dlp==2025.11.12
instagrapi==2.2.1
requests==2.32.4
python-dotenv==1.2.1
```

**الاستخدام:**
```bash
pip install -r requirements.txt
```

**الفائدة:**
- تثبيت جميع المكتبات بأمر واحد
- ضمان توافق الإصدارات
- سهولة نقل المشروع إلى جهاز آخر

---

### 6. `.env` - متغيرات البيئة

**الغرض:** تخزين البيانات الحساسة والإعدادات الخاصة

**المحتوى:**
```env
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
DOWNLOAD_FOLDER=downloads
```

**⚠️ تحذير:**
- **لا تشارك هذا الملف** - يحتوي على بيانات حساسة
- **لا تضعه على الإنترنت** - قد يسرق رمز البوت
- **أضفه إلى `.gitignore`** - لتجنب رفعه على GitHub

---

### 7. `run.sh` و `run.bat` - ملفات التشغيل

**الغرض:** تسهيل تشغيل البوت على أنظمة مختلفة

**المحتوى:**
- إنشاء البيئة الافتراضية إذا لم تكن موجودة
- تفعيل البيئة الافتراضية
- تثبيت المكتبات
- التحقق من ملف `.env`
- تشغيل البوت

**الاستخدام:**
```bash
# على Linux/Mac
./run.sh

# على Windows
run.bat
```

**الفائدة:**
- تشغيل البوت بأمر واحد
- إنشاء البيئة تلقائياً
- التحقق من الإعدادات

---

### 8. `.gitignore` - ملفات Git المتجاهلة

**الغرض:** تحديد الملفات التي لا يجب رفعها على GitHub

**المحتوى:**
- مجلد `venv/` - البيئة الافتراضية
- ملف `.env` - البيانات الحساسة
- مجلد `downloads/` - الملفات المنزلة
- ملفات النظام والسجلات

**الفائدة:**
- حماية البيانات الحساسة
- تقليل حجم المستودع
- تجنب الملفات غير الضرورية

---

### 9. ملفات التوثيق

| الملف | الغرض |
|------|-------|
| `README.md` | معلومات عامة عن المشروع والميزات |
| `QUICK_START.md` | دليل البدء السريع في 3 خطوات |
| `INSTALLATION.md` | دليل التثبيت التفصيلي لكل نظام |
| `USAGE.md` | دليل الاستخدام الشامل مع أمثلة |
| `STRUCTURE.md` | شرح هيكل المشروع (هذا الملف) |

---

## 🔄 تدفق البيانات

```
المستخدم
   ↓
تليجرام
   ↓
bot_improved.py (معالج الرسائل)
   ↓
downloader.py (تنزيل الفيديو)
   ↓
yt-dlp (تنزيل الملف)
   ↓
downloads/ (حفظ الملف)
   ↓
bot_improved.py (إرسال الملف)
   ↓
تليجرام
   ↓
المستخدم
```

---

## 🔧 كيفية التعديل والتطوير

### إضافة ميزة جديدة

1. **أضف الإعدادات** إلى `config.py`
2. **أضف الوظيفة** إلى `downloader.py` أو ملف جديد
3. **أضف معالج** إلى `bot_improved.py`
4. **اختبر الميزة** قبل النشر

### مثال: إضافة دعم منصة جديدة

```python
# 1. في config.py
SUPPORTED_PLATFORMS = {
    'youtube': {...},
    'tiktok': {...},
    'instagram': {...},
    'facebook': {...},  # منصة جديدة
}

# 2. في downloader.py
@staticmethod
def is_facebook_url(url: str) -> bool:
    return 'facebook.com' in url or 'fb.watch' in url

@staticmethod
def download_facebook(url: str) -> str:
    # كود التنزيل
    pass

# 3. في bot_improved.py
# أضف معالج جديد للمنصة
```

---

## 📊 حجم الملفات

| الملف | الحجم | النوع |
|------|-------|-------|
| `bot.py` | ~8 KB | Python |
| `bot_improved.py` | ~9 KB | Python |
| `config.py` | ~4 KB | Python |
| `downloader.py` | ~5 KB | Python |
| `README.md` | ~15 KB | Markdown |
| `INSTALLATION.md` | ~12 KB | Markdown |
| `USAGE.md` | ~14 KB | Markdown |

---

## 🔐 الأمان والخصوصية

### ملفات حساسة:
- `.env` - يحتوي على رمز البوت
- `downloads/` - قد يحتوي على ملفات خاصة

### إجراءات الأمان:
- استخدام `python-dotenv` لتحميل الإعدادات
- عدم طباعة البيانات الحساسة
- حذف الملفات المنزلة بعد الإرسال
- استخدام `.gitignore` لحماية البيانات

---

## 📈 التطور المستقبلي

### ميزات مخطط إضافتها:
- [ ] دعم منصات جديدة (Facebook, Twitter, etc.)
- [ ] واجهة ويب للتحكم بالبوت
- [ ] قاعدة بيانات متقدمة
- [ ] نظام الإشعارات
- [ ] دعم التحميل المجمع
- [ ] نظام الأذونات والأدوار

---

**آخر تحديث:** نوفمبر 2024
